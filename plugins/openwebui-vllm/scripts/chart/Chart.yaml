cat > plugins/openwebui-vllm/scripts/chart/Chart.yaml <<'EOF'
apiVersion: v2
name: openwebui-vllm
description: Packaged chart for Open WebUI + vLLM workload
type: application
version: 0.1.0
appVersion: "0.1.0"
EOF
