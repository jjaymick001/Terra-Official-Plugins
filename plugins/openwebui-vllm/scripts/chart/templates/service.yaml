apiVersion: v1
kind: Service
metadata:
  name: "{{ .Values.name }}"
  labels:
    juno-innovations.com/app: "{{ .Values.name }}"
    juno-innovations.com/workstation: "{{ .Values.name }}"
spec:
  type: {{ .Values.openwebuiServiceType }}
  selector:
    juno-innovations.com/workstation: "{{ .Values.name }}"
  ports:
    - name: viewer
      port: 80
      targetPort: 3001
      protocol: TCP
